<UserControl x:Class="CentralStationDemo.View.LocomotiveSpeedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentralStationDemo.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="km/h" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{Binding MaxSpeed}" HorizontalAlignment="Center"/>
        <Slider Grid.Row="2" Value="{Binding Velocity, Delay=500}" Minimum="0" Maximum="1000"  Orientation="Vertical" HorizontalAlignment="Center" Margin="1"/>
        <TextBlock Grid.Row="3" Text="{Binding Velocity}" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
