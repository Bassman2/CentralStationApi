<UserControl x:Class="CentralStationDemo.View.SystemAsyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentralStationDemo.View"
             xmlns:vm="clr-namespace:CentralStationDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="System Asyncron">
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Content="Stop All" Command="{Binding SystemAsyncStopAllCommand}" Margin="3" />
            <Button Grid.Column="1" Grid.Row="0" Content="Go All" Command="{Binding SystemAsyncGoAllCommand}" Margin="3" />
            <Button Grid.Column="2" Grid.Row="0" Content="Halt All" Command="{Binding SystemAsyncHaltAllCommand}" Margin="3" />
            <Button Grid.Column="3" Grid.Row="0" Content="Loco Halt All" Command="{Binding SystemAsyncLocoHaltAllCommand}" Margin="3" />
            <Button Grid.Column="4" Grid.Row="0" Content="Loco Cycle Stop All" Command="{Binding SystemAsyncLocoCycleStopAllCommand}" Margin="3" />
           
            <Button Grid.Column="0" Grid.Row="1" Content="Stop Device" Command="{Binding SystemAsyncStopDeviceCommand}" Margin="3" />
            <Button Grid.Column="1" Grid.Row="1" Content="Go Device" Command="{Binding SystemAsyncGoDeviceCommand}" Margin="3" />
            <Button Grid.Column="2" Grid.Row="1" Content="Halt Device" Command="{Binding SystemAsyncHaltDeviceCommand}" Margin="3" />
            <Button Grid.Column="3" Grid.Row="1" Content="Loco Halt Device" Command="{Binding SystemAsyncLocoHaltDeviceCommand}" Margin="3" />
            <Button Grid.Column="4" Grid.Row="1" Content="Loco Cycle Stop Device" Command="{Binding SystemAsyncLocoCycleStopDeviceCommand}" Margin="3" />


        </Grid>
    </GroupBox>
</UserControl>
