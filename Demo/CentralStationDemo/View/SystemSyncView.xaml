<UserControl x:Class="CentralStationDemo.View.SystemSyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentralStationDemo.View"
             xmlns:vm="clr-namespace:CentralStationDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="System Synchronous">
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
                <ColumnDefinition Width="*" MinWidth="80"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Content="Stop" Command="{Binding SystemStopCommand}" Margin="3" />
            <Button Grid.Column="1" Grid.Row="0" Content="Go" Command="{Binding SystemGoCommand}" Margin="3" />
            <Button Grid.Column="2" Grid.Row="0" Content="Halt" Command="{Binding SystemHaltCommand}" Margin="3" />
            <Button Grid.Column="3" Grid.Row="0" Content="Loco Halt" Command="{Binding SystemLocoHaltCommand}" Margin="3" />
            <Button Grid.Column="4" Grid.Row="0" Content="Loco Cycle Stop" Command="{Binding SystemLocoCycleStopCommand}" Margin="3" />

            

            <Label Grid.Column="1" Grid.Row="1" Content="Device Address:" />
            <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding Device, StringFormat=X4}" Margin="3" />
            <TextBlock Grid.Column="3" Grid.Row="1" Text="{Binding Device, Converter={StaticResource DeviceToDescriptionConverter}, UpdateSourceTrigger=PropertyChanged}" Margin="3" />


        </Grid>
    </GroupBox>
</UserControl>
