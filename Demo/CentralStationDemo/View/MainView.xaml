<AppWindowView x:Class="CentralStationDemo.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CentralStationDemo.View"
        xmlns:vm="clr-namespace:CentralStationDemo.ViewModel" 
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Height="800" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Content="Stop" Command="{Binding SystemStopCommand}" Margin="3" />
            <Button Grid.Column="0" Grid.Row="1" Content="Go" Command="{Binding SystemGoCommand}" Margin="3" />
            <Button Grid.Column="0" Grid.Row="2" Content="Halt" Command="{Binding SystemHaltCommand}" Margin="3" />

            <Button Grid.Column="0" Grid.Row="3" Content="LocoInfo" Command="{Binding LocoInfoCommand}" Margin="3" />
            <Button Grid.Column="0" Grid.Row="4" Content="Locos" Command="{Binding LocosCommand}" Margin="3" />

        </Grid>
        <DataGrid 
            Grid.Row="1" 
            ItemsSource="{Binding Messages}" 
            AutoGenerateColumns="False" 
            IsReadOnly="True"
            HeadersVisibility="Column"
            GridLinesVisibility="None"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeColumns="True"
            CanUserReorderColumns="True"
            CanUserSortColumns="True"
            IsSynchronizedWithCurrentItem="True"
            ClipboardCopyMode="IncludeHeader"
            FontFamily="Courier"
            
            Margin="3,3,3,3" Grid.ColumnSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="110"/>
                <DataGridTextColumn Header="Sender" Binding="{Binding Sender}" Width="110"/>
                <DataGridTextColumn Header="Binary" Binding="{Binding Binary}" Width="260"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="80"/>
                <DataGridTextColumn Header="Command" Binding="{Binding Command}" Width="140"/>
                <DataGridTextColumn Header="IsResponse" Binding="{Binding IsResponse}" Width="70"/>
                <DataGridTextColumn Header="Hash" Binding="{Binding Hash}" Width="50"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</AppWindowView>
