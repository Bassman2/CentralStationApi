<UserControl x:Class="CentralStationDemo.View.LocomotivesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentralStationDemo.View"
             xmlns:vm="clr-namespace:CentralStationDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView ItemsSource="{Binding Locomotives}" Margin="3">
        <ListView.LayoutTransform>
            <ScaleTransform ScaleX="{Binding ZoomValue}" ScaleY="{Binding ZoomValue}" />
        </ListView.LayoutTransform>
        <ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" VerticalAlignment="Top"/>
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>
        <ListView.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Sort">
                    <MenuItem Header="By Name" Command="{Binding SortLocomotivesCommand}" CommandParameter="Name"/>
                    <MenuItem Header="By Symbol" Command="{Binding SortLocomotivesCommand}" CommandParameter="Symbol"/>
                    <MenuItem Header="By UID" Command="{Binding SortLocomotivesCommand}" CommandParameter="Uid"/>
                    <MenuItem Header="By Address" Command="{Binding SortLocomotivesCommand}" CommandParameter="Address"/>
                    <MenuItem Header="By SID" Command="{Binding SortLocomotivesCommand}" CommandParameter="Sid"/>
                    <MenuItem Header="By Type" Command="{Binding SortLocomotivesCommand}" CommandParameter="Type"/>
                    <MenuItem Header="By MFX Type" Command="{Binding SortLocomotivesCommand}" CommandParameter="MfxType"/>
                    <MenuItem Header="By MFX UID" Command="{Binding SortLocomotivesCommand}" CommandParameter="MfxUid"/>
                </MenuItem>
            </ContextMenu>
        </ListView.ContextMenu>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel>
                    <local:LocomotiveInfoView/>
                    <local:LocomotiveControlView/>
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ListView>
</UserControl>
