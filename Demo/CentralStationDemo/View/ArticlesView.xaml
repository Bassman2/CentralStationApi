<UserControl x:Class="CentralStationDemo.View.ArticlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentralStationDemo.View"
             xmlns:vm="clr-namespace:CentralStationDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="Articles">
        <DataGrid 
            Grid.Row="4" 
            ItemsSource="{Binding MagneticItems}" 
            AutoGenerateColumns="False" 
            IsReadOnly="True"
            HeadersVisibility="Column"
            GridLinesVisibility="None"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeColumns="True"
            CanUserReorderColumns="True"
            CanUserSortColumns="True"
            IsSynchronizedWithCurrentItem="True"
            ClipboardCopyMode="IncludeHeader">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="SwitchingTime" Binding="{Binding SwitchingTime}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Odd" Binding="{Binding Odd}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="DecoderType" Binding="{Binding DecoderType}" Width="110" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </GroupBox>
</UserControl>
